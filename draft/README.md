# draft/ — not in the gallery

Packs here are finished enough to test but deliberately **not** published:
`site/generate.py` builds the gallery from `official/` only, so nothing in
this directory can be installed by anyone browsing yat.day.

CI still validates and render-tests them, so parked work does not rot.

- **`todo`** — the voice-updatable household list. Parked 2026-08-07 pending
  hardware testing of the voice path; requires firmware ≥0.4.0 for its
  intents (`min_firmware`). Move back to `official/` when the owner has
  tested it end to end with a microphone.
